// These are zero because we have hartree fock orbitals
//( + 1.0 ) *  Fai["kd"]     * Rabij["dbki"]

( + 0.5 ) *  Vabij["klie"] * Rabij["ebkl"]  // H21 (kle) -> bi
( + 0.5 ) *  Vabij["kbde"] * Rabij["deki"]  // H21 (dek) -> bi

